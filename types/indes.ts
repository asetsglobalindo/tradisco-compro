export interface LocationType {
  created_at: string;
  _id: string;
  name: string;
  slug: string;
  code: string;
  postal_code: number;
  address: string;
  lat: string;
  long: string;
  vr_url: string | null;
  publish: number;
}

